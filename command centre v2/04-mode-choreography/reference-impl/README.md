# Reference implementations of Mode 3

Drop reference coordinator implementations here. Each subfolder is one impl.
Each impl must include its own README and a conformance test against
[../contract.md](../contract.md).
